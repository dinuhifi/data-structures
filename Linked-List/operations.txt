1. create(arr[], len) // we use a temp pointer & last pointer of type "Node" to traverse the arr and keep track of the elements.
2. display(*first) // *first is the pointer pointing to the first element of linked list.
3. recursiveDisplay(*first)